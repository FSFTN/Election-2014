<legend class="text-center welcome"> <h2>Welcome to Election Watch</h2></legend>
<div class="col-md-12 col-sm-12 col-xs-12 search-container">
  <div class="col-md-8 col-md-offset-2 col-sm-10 col-sm-offset-1 col-xs-10 col-xs-offset-1">
    <%= simple_form_for :search, :url => search_path, :method=> "get" do |f| %>
    <div class="col-md-5 col-sm-12 col-xs-12 state">
      <%= f.collection_select :state_id, State.order(:name),  :id, :name, {:include_blank=> "Select a State"}, {:class=>'dropdown'} %>
    </div>
    <div class="col-md-5 col-sm-12  col-xs-12 constituency">
      <%= f.grouped_collection_select :constituency_id, State.order(:name), :constituencies, :name, :id, :name, {:include_blank=> "Select a Constituency"}, {:class=>'dropdown'} %>
    </div>
    <div class="col-md-2">
      <%= f.submit "Search", class: "btn btn-success search_button ", id: "search"%>
    </div>
    <% end %>
    <div class="text-center explanation  ">
      <legend>
        This Election Season, are you confused with all the news? 
      </legend>
      <legend>Are you looking for a one-stop source of information?</legend>
      <legend>Want to find out what each candidate stands for in their manifestos? </legend>
      <legend>Your search ends here.</legend>
    </div>
  </div>
</div>
